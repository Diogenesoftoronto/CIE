[project]
name = "cie"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "textual>=0.47",
    "textualize>=0.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]
